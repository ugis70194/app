import React from 'react'

interface props {
  project: string,
  product: string,
  count: number,
}

export default function SampleViewer({project, product, count}: props){
  return (
    <>
    </>
  )
}